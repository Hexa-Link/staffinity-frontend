---
// Página: My Profile - Vista del Empleado (con edición de datos personales)
import DashboardLayout from '../layouts/DashboardLayout.astro';
---

<DashboardLayout title="Mi Perfil - HexaLink ERP">
  <style>
    .profile-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
    }

    .profile-header {
      background: linear-gradient(135deg, #0d9488 0%, #0e7490 100%);
      color: white;
      padding: 2rem;
      border-radius: 12px;
      margin-bottom: 2rem;
      display: flex;
      align-items: center;
      gap: 2rem;
      justify-content: space-between;
    }

    .profile-header-info {
      display: flex;
      align-items: center;
      gap: 2rem;
      flex: 1;
    }

    .profile-avatar {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.2);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 3rem;
      flex-shrink: 0;
    }

    .profile-info h1 {
      font-size: 2rem;
      margin: 0 0 0.5rem 0;
    }

    .profile-info p {
      margin: 0.25rem 0;
      opacity: 0.95;
    }

    .profile-badge {
      display: inline-block;
      background: rgba(255, 255, 255, 0.2);
      padding: 0.5rem 1rem;
      border-radius: 50px;
      font-size: 0.85rem;
      margin-top: 0.5rem;
    }

    .btn-edit-profile {
      background: white;
      color: #0d9488;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
      white-space: nowrap;
    }

    .btn-edit-profile:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
    }

    .profile-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin-bottom: 2rem;
    }

    .profile-card {
      background: #1e293b;
      border: 1px solid #334155;
      border-radius: 12px;
      padding: 2rem;
    }

    .profile-card h3 {
      color: #0d9488;
      font-size: 1.3rem;
      margin: 0 0 1rem 0;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .profile-field {
      margin-bottom: 1.5rem;
    }

    .profile-field label {
      color: #94a3b8;
      font-size: 0.875rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      display: block;
      margin-bottom: 0.5rem;
    }

    .profile-field value {
      color: #f1f5f9;
      font-size: 1rem;
      display: block;
      padding: 0.75rem;
      background: #0f172a;
      border-radius: 6px;
      border: 1px solid #334155;
    }

    .status-badge {
      display: inline-block;
      padding: 0.4rem 0.8rem;
      border-radius: 50px;
      font-size: 0.8rem;
      font-weight: 600;
      background: #dcfce7;
      color: #166534;
    }

    .status-badge.vacation {
      background: #fef3c7;
      color: #92400e;
    }

    .status-badge.inactive {
      background: #e5e7eb;
      color: #374151;
    }

    /* Edit Modal */
    .modal {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.7);
      z-index: 1000;
      align-items: center;
      justify-content: center;
    }

    .modal.active {
      display: flex;
    }

    .modal-content {
      background: #1e293b;
      border: 1px solid #334155;
      border-radius: 12px;
      padding: 2rem;
      max-width: 600px;
      width: 90%;
      max-height: 80vh;
      overflow-y: auto;
    }

    .modal-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }

    .modal-header h2 {
      color: #f1f5f9;
      margin: 0;
    }

    .modal-close {
      background: none;
      border: none;
      color: #cbd5e1;
      font-size: 1.5rem;
      cursor: pointer;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      color: #cbd5e1;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .form-group input,
    .form-group select {
      width: 100%;
      padding: 0.75rem;
      background: #0f172a;
      border: 1px solid #334155;
      border-radius: 6px;
      color: #f1f5f9;
      font-size: 1rem;
    }

    .form-group input:focus,
    .form-group select:focus {
      outline: none;
      border-color: #0d9488;
    }

    .modal-actions {
      display: flex;
      gap: 1rem;
      justify-content: flex-end;
    }

    .btn-primary {
      background: #0d9488;
      color: white;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 6px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .btn-primary:hover {
      background: #0e7490;
    }

    .btn-secondary {
      background: #334155;
      color: #cbd5e1;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 6px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .btn-secondary:hover {
      background: #475569;
    }

    @media (max-width: 768px) {
      .profile-header {
        flex-direction: column;
        align-items: flex-start;
      }

      .profile-header-info {
        flex-direction: column;
      }

      .profile-grid {
        grid-template-columns: 1fr;
      }

      .modal-content {
        width: 95%;
      }
    }
  </style>

  <div class="profile-container">
    <!-- Profile Header -->
    <div class="profile-header">
      <div class="profile-header-info">
        <div class="profile-avatar">👤</div>
        <div class="profile-info">
          <h1>Juan Pérez García</h1>
          <p>ID: EMP-001</p>
          <p>Senior Backend Developer</p>
          <p>Engineering</p>
          <div class="profile-badge">
            <span class="status-badge">✓ Activo</span>
          </div>
        </div>
      </div>
      <button class="btn-edit-profile" id="btnEditProfile">✏️ Editar</button>
    </div>

    <!-- Profile Cards -->
    <div class="profile-grid">
      <!-- Personal Information -->
      <div class="profile-card">
        <h3>👤 Información Personal</h3>
        <div class="profile-field">
          <label>Nombre Completo</label>
          <value>Juan Pérez García</value>
        </div>
        <div class="profile-field">
          <label>Email</label>
          <value>juan.perez@hexalink.com</value>
        </div>
        <div class="profile-field">
          <label>Teléfono</label>
          <value>+57 3123456789</value>
        </div>
        <div class="profile-field">
          <label>Fecha de Nacimiento</label>
          <value>15/03/1990</value>
        </div>
      </div>

      <!-- Work Information -->
      <div class="profile-card">
        <h3>💼 Información Laboral</h3>
        <div class="profile-field">
          <label>Posición</label>
          <value>Senior Backend Developer</value>
        </div>
        <div class="profile-field">
          <label>Departamento</label>
          <value>Engineering</value>
        </div>
        <div class="profile-field">
          <label>Fecha de Ingreso</label>
          <value>01/06/2021</value>
        </div>
        <div class="profile-field">
          <label>Salario (Base)</label>
          <value>*** Información Protegida ***</value>
        </div>
      </div>

      <!-- Contact Information -->
      <div class="profile-card">
        <h3>📞 Información de Contacto</h3>
        <div class="profile-field">
          <label>Teléfono Emergencia</label>
          <value>+57 3187654321</value>
        </div>
        <div class="profile-field">
          <label>Dirección</label>
          <value>Cra 53 #85-45, Barranquilla</value>
        </div>
        <div class="profile-field">
          <label>Código Postal</label>
          <value>080001</value>
        </div>
        <div class="profile-field">
          <label>Ciudad</label>
          <value>Barranquilla</value>
        </div>
      </div>
    </div>
  </div>

  <!-- Edit Modal -->
  <div class="modal" id="editModal">
    <div class="modal-content">
      <div class="modal-header">
        <h2>Editar Información Personal</h2>
        <button class="modal-close" id="closeModal">×</button>
      </div>

      <form id="editForm">
        <div class="form-group">
          <label>Email</label>
          <input type="email" value="juan.perez@hexalink.com" required>
        </div>

        <div class="form-group">
          <label>Teléfono</label>
          <input type="tel" value="+57 3123456789">
        </div>

        <div class="form-group">
          <label>Teléfono de Emergencia</label>
          <input type="tel" value="+57 3187654321">
        </div>

        <div class="form-group">
          <label>Dirección</label>
          <input type="text" value="Cra 53 #85-45, Barranquilla">
        </div>

        <div class="form-group">
          <label>Código Postal</label>
          <input type="text" value="080001">
        </div>

        <div class="form-group">
          <label>Ciudad</label>
          <input type="text" value="Barranquilla">
        </div>

        <div class="modal-actions">
          <button type="button" class="btn-secondary" id="cancelBtn">Cancelar</button>
          <button type="submit" class="btn-primary">Guardar Cambios</button>
        </div>
      </form>

      <p style="margin-top: 1rem; color: #94a3b8; font-size: 0.85rem;">
        ℹ️ Nota: Algunos datos como nombre completo, posición y departamento no pueden ser editados. Contacta a Recursos Humanos para cambios en tu información laboral.
      </p>
    </div>
  </div>

  <script>
    const btnEdit = document.getElementById('btnEditProfile');
    const editModal = document.getElementById('editModal');
    const closeBtn = document.getElementById('closeModal');
    const cancelBtn = document.getElementById('cancelBtn');
    const editForm = document.getElementById('editForm');

    btnEdit?.addEventListener('click', () => {
      editModal?.classList.add('active');
    });

    closeBtn?.addEventListener('click', () => {
      editModal?.classList.remove('active');
    });

    cancelBtn?.addEventListener('click', () => {
      editModal?.classList.remove('active');
    });

    editForm?.addEventListener('submit', (e: SubmitEvent) => {
      e.preventDefault();
      alert('✅ Cambios guardados exitosamente');
      editModal?.classList.remove('active');
    });

    // Close modal when clicking outside
    editModal?.addEventListener('click', (e: Event) => {
      if (e.target === editModal) {
        editModal.classList.remove('active');
      }
    });
  </script>
</DashboardLayout>
