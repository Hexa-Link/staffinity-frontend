---
// Widget de estadísticas
interface Props {
  title: string;
  value: string | number;
  subtitle?: string;
  icon?: string;
  trend?: 'up' | 'down' | 'neutral';
  trendValue?: string;
}

const { title, value, subtitle, icon = '📊', trend = 'neutral', trendValue } = Astro.props;

const trendColor = trend === 'up' ? 'text-green-600' : trend === 'down' ? 'text-red-600' : 'text-[#6b7c7c]';
const trendBg = trend === 'up' ? 'bg-green-50' : trend === 'down' ? 'bg-red-50' : 'bg-[#eef6f6]';
---

<div class="bg-white border-2 border-[#c7dfdf] rounded-[14px] p-6 hover:shadow-[0_12px_32px_rgba(12,43,43,0.15)] transition-all duration-300">
  <div class="flex items-start justify-between mb-4">
    <div>
      <p class="text-sm text-[#6b7c7c] font-medium mb-1">{title}</p>
      <h3 class="text-3xl font-bold text-[#0b0b0b]">{value}</h3>
      {subtitle && <p class="text-xs text-[#6b7c7c] mt-1">{subtitle}</p>}
    </div>
    <div class="text-3xl">{icon}</div>
  </div>
  
  {trendValue && (
    <div class={`inline-block px-3 py-1 rounded-full text-xs font-semibold ${trendBg} ${trendColor}`}>
      {trend === 'up' ? '↑' : trend === 'down' ? '↓' : '→'} {trendValue}
    </div>
  )}
</div>
